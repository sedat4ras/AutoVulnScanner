# Ollama configuration (local, no API key needed)
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=mistral

# Optional: if using Claude API instead of local Ollama
# OPENAI_API_KEY=sk-...
# USE_CLAUDE=false
